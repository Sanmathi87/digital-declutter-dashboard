apiVersion: v1
kind: Service
metadata:
  name: declutter-dashboard-service
spec:
  type: NodePort
  selector:
    app: declutter-dashboard
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30500